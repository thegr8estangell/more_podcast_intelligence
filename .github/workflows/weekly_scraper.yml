name: Weekly Podcast Intelligence

on:
  schedule:
    - cron: "0 13 * * 1"
  workflow_dispatch:

jobs:
  scrape-and-report:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libatk1.0-0t64 libatk-bridge2.0-0t64 libcups2t64 libdrm2 libatspi2.0-0t64 libxcomposite1 libxdamage1 libxfixes3 libxrandr2 libgbm1 libxkbcommon0 libasound2t64

      - name: Install Python dependencies
        run: |
          pip install -r requirements.txt
          PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=0 playwright install chromium --with-deps=false

      - name: Run scraper
        env:
          SPOTIFY_CLIENT_ID:     ${{ secrets.SPOTIFY_CLIENT_ID }}
          SPOTIFY_CLIENT_SECRET: ${{ secrets.SPOTIFY_CLIENT_SECRET }}
          PODCASTINDEX_KEY:      ${{ secrets.PODCASTINDEX_KEY }}
          PODCASTINDEX_SECRET:   ${{ secrets.PODCASTINDEX_SECRET }}
          GMAIL_EMAIL:           ${{ secrets.GMAIL_EMAIL }}
          GMAIL_PASSWORD:        ${{ secrets.GMAIL_PASSWORD }}
          REPORT_EMAIL:          ${{ secrets.REPORT_EMAIL }}
        run: python scraper.py

      - name: Send report
        env:
          GMAIL_EMAIL:    ${{ secrets.GMAIL_EMAIL }}
          GMAIL_PASSWORD: ${{ secrets.GMAIL_PASSWORD }}
          REPORT_EMAIL:   ${{ secrets.REPORT_EMAIL }}
        run: python send_report.py

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: podcast-data-${{ github.run_id }}
          path: |
            podcast_data.json
            email_preview.html
          retention-days: 14
